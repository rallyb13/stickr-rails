<h2>Edit <%= @user.name %> info </h2>

<%= render 'form' %>

<h5>Your Avatar</h5>
<%= form_for @user, :url => user_path(@user), :html => { :multipart => true} do | form | %>
  <%= form.file_field :avatar %>
  <%= form.submit "Addvatar" %>
<% end %>
